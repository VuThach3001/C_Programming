#include <stdio.h>
/* #include & #define directives... */


/* Function prototypes */
double Average(double data_values[], size_t count);
double Sum(double *x, size_t n);
size_t GetData(double*, size_t);


int main()
{
    /* Code for main() */
    return 0;
}

// Definitions/implementation for Average(), Sum() and GetData()...